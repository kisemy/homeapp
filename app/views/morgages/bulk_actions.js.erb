swal({
    icon: "success",
    title: 'Success',
    buttons: false,
    timer: 3000,
    text: "<%= t('views.morgages.bulk_action_msg', action: @action) %>"}).then(function(){
    window.location = '<%= morgages_path %>'
});
