<%= form_with(model: glentry, local: true) do |form| %>
  <% if glentry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glentry.errors.count, "error") %> prohibited this glentry from being saved:</h2>

      <ul>
        <% glentry.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
